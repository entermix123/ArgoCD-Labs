apiVersion: v1
kind: Pod
metadata:
  name: quick-fail-pod
spec:
  containers:
  - name: nginx
    image: nginx
    resources:
      limits:
        memory: "invalid-value"  # ❌ This fails validation immediately during apply